<Window x:Class="WpfApp6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition/>
            <RowDefinition Height="0.6*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Авторизация" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <StackPanel HorizontalAlignment="Center" Margin="0,103,0,0" Grid.RowSpan="2" Width="386" VerticalAlignment="Center">
            <TextBlock Text="Логин" FontSize="20"/>
            <TextBox x:Name="LoginField" Height="30"/>

            <TextBlock Text="Пароль" FontSize="20"/>
            <TextBox x:Name="PasswordField" Height="30"/>
        </StackPanel>

        <Button x:Name="Auth_btn" Grid.Row="2" Content="Войти" Margin="100, 40" Click="Auth_btn_Click"/>
    </Grid>
</Window>
